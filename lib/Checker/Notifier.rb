class Notifier

  # A Message is the primary content of the message
  # The Type (good, warning, danger) determines the severity of the message
  # the attachments array is a list of extra messages that may or may not be sent
  def send(message, type = '', attachments = [])
    
  end

end
